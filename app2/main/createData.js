const createData = (createClick) => {
        <form onClick={}>
            <label>x:</label>
            <label>y:</label>
            <button>Submit</button>
        </form>
}

export default createData;